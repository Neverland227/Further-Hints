hardware:
  cpu: AMD EPYC Turin
  physical_cores: 64
  ram_gb: 128
  reserved_ram_gb: 24
threading:
  blas_threads_per_worker: 1
synthetic_pool:
  workers: 48
  max_tasks_per_child: 2
  estimated_rss_gb_per_worker: 1
lattice_pool:
  workers: 6
  max_tasks_per_child: 1
  max_RSS_per_worker: 10GB
  max_wall_time_per_job: 900
sage_pool:
  enabled: false
  workers_if_explicitly_enabled: 4
  max_tasks_per_child: 1
  max_RSS_per_worker: 12GB
  max_wall_time_per_job: 600
policy:
  checkpoint_each_instance: true
  atomic_merge: true
  no_automatic_grid_expansion: true
  no_automatic_beta_increase: true
  g6k: DEFERRED_OUT_OF_SCOPE

